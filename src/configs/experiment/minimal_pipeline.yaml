algorithms:
  - _target_: src.algorithms.pca.PCAModule
    n_components: 2
    random_state: 42
  - _target_: src.algorithms.autoencoder.AutoencoderModule
    input_dim: 5
    latent_dim: 2
    hidden_dims: [8, 8]
    activation: relu
    batchnorm: false
    dropout: 0.0
    # Add any other required AE params here
trainer:
  max_epochs: 1
  gpus: 0
callbacks:
  embedding: {} 
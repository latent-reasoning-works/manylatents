# @package _global_

defaults:
  - override /algorithm/dimensionality_reduction: pca
  - override /data: swissroll
  - override /metrics:
      - diffusion_curvature

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 2

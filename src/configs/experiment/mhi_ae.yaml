# @package _global_
name: mhi_ae_MSELoss_50D

defaults:
  - override /algorithm/model: ae_reconstruction
  - override /data: mhi_split
  - override /callbacks/embedding: default
  - override /trainer: default
  - override /metrics: mhi_dr

algorithm:
  model:
    network:
      input_dim: 197675 # MHI ncols     
      
seed: 42

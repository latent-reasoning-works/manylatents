# @package _global_

defaults:
  - override /algorithm: default
  - override /algorithm/dimensionality_reduction: phate
  - override /datamodule: hgdp
  - override /paths: shuang_path

name: null

seed: 42
# @package _global_
name: recent_simulated_genetic_pca_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: pca
  - override /data: recent_simulated_genetic
  - override /callbacks/embedding: default
  - override /metrics: synthetic_data_metrics_plus_admixture #default

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 2

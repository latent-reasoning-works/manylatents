# @package _global_
name: hgdp_phate_full

defaults:
  - override /algorithm: default
  - override /algorithm/dimensionality_reduction: phate
  - override /datamodule: hgdp_pca_precomputed
  - override /callbacks/dimensionality_reduction: default
  - override /metrics: hgdp_dr
  
datamodule:
  mode: full

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 2
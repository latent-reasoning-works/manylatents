# @package _global_
name: hgdp_dr_noop

defaults:
  - override /algorithm/dimensionality_reduction: noop
  - override /data: hgdp_split
  - override /callbacks/dimensionality_reduction: default
  - override /metrics: tangent_space

seed: 42

data:
  filter_related: true # consistency with hgdp_pca, turn off if running from other sources

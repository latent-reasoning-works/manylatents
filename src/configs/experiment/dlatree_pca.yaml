# @package _global_
name: dlatree_pca_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: pca
  - override /data: dlatree
  - override /callbacks/embedding: default
  - override /metrics: synthetic_data_metrics_suite #synthetic_data_metrics

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 2
    
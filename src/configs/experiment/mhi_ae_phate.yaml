# @package _global_
name: mhi_ae_phate_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: phate
  - override /data: mhi_split
  - override /callbacks/embedding: default
  - override /metrics: mhi_dr
  
algorithm:
  dimensionality_reduction:
    n_components: 2

callbacks:
  embedding:
    plot_embeddings:
      label_col: label
      
data:
  precomputed_path: "./data/embeddings/mhi_ae.csv"

seed: 42
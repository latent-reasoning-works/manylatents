# @package _global_
name: swissroll_pca_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: pca
  - override /data: swissroll
  - override /callbacks/embedding: default
  - override /metrics: synthetic_data_metrics

seed: 42
project: manifold_genetics

algorithm:
  dimensionality_reduction:
    n_components: 2
    
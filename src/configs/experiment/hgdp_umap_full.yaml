# @package _global_
name: hgdp_umap_full_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm: default
  - override /algorithm/dimensionality_reduction: umap
  - override /datamodule: hgdp
  - override /callbacks/dimensionality_reduction: default
  - override /metrics: hgdp_dr
  
datamodule:
  mode: full

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 2
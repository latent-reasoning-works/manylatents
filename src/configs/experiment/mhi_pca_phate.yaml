# @package _global_
name: mhi_pca_phate

defaults:
  - override /algorithm/dimensionality_reduction: phate
  - override /data: mhi_split
  - override /callbacks/embedding: default
  - override /metrics: mhi_dr

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 2

data:
  precomputed_path: "./data/embeddings/mhi_split_pca.csv"
  filter_related: false # no fitting on related samples

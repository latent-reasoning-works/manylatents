# @package _global_
name: custom_simulated_genetic_pca_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: pca
  - override /data: custom_simulated_genetic
  - override /callbacks/embedding: default
  - override /metrics: synthetic_data_metrics #default

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 2

# @package _global_
name: hgdp_pca_full # logging purposes

defaults:
  - override /algorithm: default
  - override /algorithm/dimensionality_reduction: pca
  - override /datamodule: hgdp
  - override /callbacks/dimensionality_reduction: default

datamodule:
  mode: full

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 2
# @package _global_
name: hgdp_umap_full_knn=${algorithm.dimensionality_reduction.knn}

defaults:
  - override /algorithm: default
  - override /algorithm/dimensionality_reduction: umap
  - override /datamodule: hgdp
  - override /callbacks/dimensionality_reduction: default

datamodule:
  mode: full

seed: 42

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      algorithm.dimensionality_reduction.knn:  3, 10, 50, 500, 1000, 2000, 3000, 4149
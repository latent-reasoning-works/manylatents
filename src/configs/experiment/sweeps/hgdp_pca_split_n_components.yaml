# @package _global_

defaults:
  - override /algorithm: default
  - override /algorithm/dimensionality_reduction: pca
  - override /datamodule: hgdp
  - override /callbacks/dimensionality_reduction: save_embeddings

datamodule:
  mode: split

seed: 42

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      algorithm.dimensionality_reduction.n_components: 2,50
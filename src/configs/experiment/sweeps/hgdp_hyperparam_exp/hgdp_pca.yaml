# @package _global_
name: hgdp_pca_full_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: pca
  - override /data: hgdp_split
  - override /callbacks/embedding: default
  - override /metrics: hgdp_hyperparameter_exp

seed: 42

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      algorithm.dimensionality_reduction.n_components: 2,50
# @package _global_
name: eb_pca_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: pca
  - override /data: embryoidbody
  - override /callbacks/embedding: default
  - override /metrics: fidelity_metrics

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 50
    

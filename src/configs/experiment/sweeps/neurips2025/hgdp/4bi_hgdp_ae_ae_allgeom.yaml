# @package _global_
name: hgdp_ae_ae_allgeom

defaults:
  - override /algorithm/model: ae_reconstruction_allgeom
  - override /data: hgdp_ae_precomputed
  - override /callbacks/embedding: default
  - override /metrics: fidelity_metrics

algorithm:
  model:
    network:
      input_dim: 50
    loss:
      loss_tsa:
        input_dim: 50

seed: 42
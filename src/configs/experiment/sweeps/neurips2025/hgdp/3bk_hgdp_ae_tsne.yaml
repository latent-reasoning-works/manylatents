# @package _global_
name: hgdp_ae_tsne_n_components=${algorithm.dimensionality_reduction.n_components}_perplexity=${algorithm.dimensionality_reduction.perplexity}

defaults:
  - override /algorithm/dimensionality_reduction: tsne
  - override /data: hgdp_ae_precomputed
  - override /callbacks/embedding: default
  - override /metrics: fidelity_metrics
    
hydra:
  mode: MULTIRUN
  sweeper:
    params:
      algorithm.dimensionality_reduction.n_components: 2, 3
      algorithm.dimensionality_reduction.perplexity: 17 #5, 8, 17, 33, 83, 167, 333

seed: 42
# @package _global_
name: mbyl_dlatree_default_ae_PRLoss_50D

defaults:
  - override /algorithm/model: ae_reconstruction_pr
  - override /data: dlatree_default
  - override /callbacks/embedding: default
  - override /trainer: default
  - override /metrics: fidelity_metrics

algorithm:
  model:
    network:
      input_dim: 200
  dimensionality_reduction:
    n_components: 50

seed: 42


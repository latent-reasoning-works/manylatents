# @package _global_
name: mbyl_dlatree_default_pca_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: pca
  - override /data: dlatree_default
  - override /callbacks/embedding: default
  - override /metrics: fidelity_metrics

seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 50

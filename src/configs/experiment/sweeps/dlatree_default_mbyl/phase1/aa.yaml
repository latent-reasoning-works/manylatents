# @package _global_
name: mbyl_dlatree_default_aa_n_archetypes=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: aa
  - override /data: dlatree_default
  - override /callbacks/embedding: default
  - override /metrics: fidelity_metrics

algorithm:
  model:
    network:
      input_dim: 200
  dimensionality_reduction:
    n_components: 50

seed: 42

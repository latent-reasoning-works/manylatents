# @package _global_
name: hgdp_phate_full_knn=${algorithm.dimensionality_reduction.knn}

defaults:
  - override /algorithm/dimensionality_reduction: phate
  - override /data: hgdp_pca_precomputed
  - override /callbacks/dimensionality_reduction: default
  - override /metrics: hgdp_dr
  
seed: 42

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      algorithm.dimensionality_reduction.knn: 3, 5, 10, 20, 50, 80, 100, 200, 500, 750, 1000, 1250, 1500, 2000, 3000, 4149
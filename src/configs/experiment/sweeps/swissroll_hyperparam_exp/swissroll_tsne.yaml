# @package _global_
name: tsne_perplexity=${algorithm.dimensionality_reduction.perplexity}

defaults:
  - /launcher: mila_cpu_cluster
  - override /algorithm/dimensionality_reduction: tsne
  - override /data: swissroll
  - override /callbacks/embedding: default
  - override /metrics: synthetic_data_metrics

seed: 42

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      algorithm.dimensionality_reduction.perplexity: 2,17,33,167,250,333,667,1000,1500,2000,4000,6000

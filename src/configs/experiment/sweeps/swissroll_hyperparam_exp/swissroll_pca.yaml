# @package _global_
name: swissroll_pca_full_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - /launcher: mila_cluster
  - override /algorithm/dimensionality_reduction: pca
  - override /data: swissroll
  - override /callbacks/embedding: default
  - override /metrics: synthetic_data_metrics

seed: 42

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      algorithm.dimensionality_reduction.n_components: 2,4
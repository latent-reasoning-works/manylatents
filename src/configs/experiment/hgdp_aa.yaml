# @package _global_
name: hgdp_aa_n_archetypes=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: aa
  - override /data: hgdp_split
  - override /callbacks/embedding: default
  - override /metrics: tangent_space

seed: 42

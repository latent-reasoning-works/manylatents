# @package _global_
name: hgdp_pca_ae

defaults:
  #- override /algorithm/dimensionality_reduction: noop
  - override /algorithm/model: ae_reconstruction
  - override /data: hgdp_pca_precomputed
  - override /callbacks/embedding: default
  - override /callbacks/trainer: default
  - override /trainer: default
  - override /metrics: fidelity_metrics

algorithm:
  model:
    network:
      input_dim: 50 # PCA dims cols x hgdp n_rows 

seed: 42
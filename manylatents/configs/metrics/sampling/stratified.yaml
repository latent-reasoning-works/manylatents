# Stratified sampling - preserves label distribution
# Usage: metrics.sampling=sampling/stratified
sampling:
  _target_: manylatents.utils.sampling.StratifiedSampling
  stratify_by: population_label
  seed: 42
  fraction: 0.1

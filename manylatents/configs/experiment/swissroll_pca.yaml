# @package _global_
name: swissroll_pca_n_components=${algorithm.latent.n_components}

defaults:
  - override /algorithm/latent: pca
  - override /data: swissroll
  - override /callbacks/embedding: default
  - override /metrics: synthetic_data_metrics

seed: 42
project: manifold_genetics

algorithm:
  latent:
    n_components: 2
    
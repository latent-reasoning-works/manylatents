# @package _global_
name: hgdp_pca_tsne_n_components=${algorithm.dimensionality_reduction.n_components}

defaults:
  - override /algorithm/dimensionality_reduction: tsne
  - override /data: hgdp_pca_precomputed
  - override /callbacks/embedding: default
  - override /metrics: hgdp_dr
  
seed: 42

algorithm:
  dimensionality_reduction:
    n_components: 2
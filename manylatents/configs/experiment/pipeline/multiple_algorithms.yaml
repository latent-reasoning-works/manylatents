# @package _global_
# Generic sequential pipeline example: PCA → Autoencoder
name: multiple_algorithms

defaults:
  - override /data: swissroll
  - override /callbacks/embedding: default
  - override /trainer: default
  - override /metrics: test_metric
  - _self_

seed: 42
project: your_project

pipeline:
  - latent/pca
  - lightning/ae_reconstruction:
      network:
        latent_dim: 2
        input_dim: 2 

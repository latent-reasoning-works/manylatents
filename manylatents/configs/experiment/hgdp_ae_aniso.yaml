# @package _global_
name: hgdp_ae_AnisotropyLoss_50D

defaults:
  - override /algorithm/model: ae_reconstruction_aniso
  - override /data: hgdp_split
  - override /callbacks/embedding: default
  - override /trainer: default
  - override /metrics: fidelity_metrics

algorithm:
  model:
    network:
      input_dim: 172152 # HGDP ncols
    loss:
      loss_tsa:
        input_dim: 172152
        
seed: 42

# @package _global_
name: single_algorithm_pca_swissroll

defaults:
  - override /algorithms/latent: pca
  - override /data: swiss_roll
  - override /callbacks/embedding: default
  - override /metrics: test_metric

seed: 42
project: spectral_clustering

algorithms:
  latent:
    n_components: 2
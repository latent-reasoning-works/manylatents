# @package _global_
name: swissroll_mds_n_components=${algorithm.dimensionality_reduction.ndim}

defaults:
  - override /algorithm/dimensionality_reduction: mds
  - override /data: swissroll
  - override /callbacks/embedding: default
  - override /metrics: synthetic_data_metrics

seed: 42

algorithm:
  dimensionality_reduction:
    ndim: 2
    
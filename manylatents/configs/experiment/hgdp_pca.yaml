# @package _global_
name: hgdp_pca_n_components=${algorithms.latent.n_components}

defaults:
  - override /algorithms/latent: pca
  - override /data: hgdp_split
  - override /callbacks/embedding: default
  - override /metrics: hgdp_dr

seed: 42

algorithms:
  latent:
    n_components: 50
    
data:
  filter_related: true # no fitting on related samples

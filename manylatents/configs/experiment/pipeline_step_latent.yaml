# @package _global_
# Generic pipeline step for latent/dimensionality reduction algorithms
name: pipeline_step_latent

defaults:
  - override /algorithms/latent: null  # To be specified in pipeline config
  - override /callbacks/embedding: default  # Includes save_embeddings, plot_embeddings, wandb_log_scores
  - override /metrics: test_metric
  - _self_

seed: 42
project: your_project
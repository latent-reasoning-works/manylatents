# @package _global_
name: hgdp_pca_ae_aniso

defaults:
  - override /algorithm/model: ae_reconstruction_aniso
  - override /data: hgdp_pca_precomputed
  - override /callbacks/embedding: default
  - override /metrics: fidelity_metrics

algorithm:
  model:
    network:
      input_dim: 50
    loss:
      loss_tsa:
        input_dim: 50

seed: 42